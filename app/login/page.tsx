"use client";
import { useState } from "react";
import Link from "next/link";
import { MarketingNav } from "@/components/marketing-nav";
import { createClient } from "@/lib/supabase/client";
export default function Login(){const [email,setEmail]=useState("");const [message,setMessage]=useState("");const [loading,setLoading]=useState(false);async function signIn(){const supabase=createClient();if(!email.includes("@")){setMessage("Introduza um e-mail válido.");return;}if(!supabase){setMessage("O Supabase ainda não está configurado neste ambiente. Pare e volte a iniciar o site no Terminal.");return;}setLoading(true);setMessage("");const {error}=await supabase.auth.signInWithOtp({email,options:{emailRedirectTo:`${window.location.origin}/auth/callback`}});setLoading(false);setMessage(error?`O Supabase devolveu: ${error.message}`:"Enviámos-lhe um link seguro. Abra o seu e-mail para entrar.");}return <><MarketingNav/><main className="shell" style={{maxWidth:500,paddingTop:65}}><section className="card" style={{padding:32}}><div className="eyebrow">Acesso seguro</div><h1 style={{margin:'10px 0 8px',fontSize:28}}>Entre na sua conta</h1><p style={{margin:'0 0 25px',color:'#68768b',fontSize:14}}>Receberá um link de entrada no seu e-mail.</p><label style={{fontSize:13,fontWeight:700}}>E-mail profissional</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="nome@empresa.pt" style={{width:'100%',marginTop:7,padding:12,border:'1px solid #cad4e0',borderRadius:8,fontSize:14}}/><button onClick={signIn} disabled={loading} className="btn btn-primary" style={{width:'100%',marginTop:16,opacity:loading?.7:1}}>{loading?"A enviar…":"Enviar link de acesso"}</button>{message&&<p role="status" style={{fontSize:13,color:'#38526e',lineHeight:1.5,marginTop:18}}>{message}</p>}<p style={{fontSize:12,color:'#7b8797',lineHeight:1.5,marginTop:18}}>O acesso é feito pelo Supabase e o link expira automaticamente.</p><Link href="/dashboard" style={{display:'block',textAlign:'center',fontSize:12,color:'#6e7c90',marginTop:12}}>Ver a demonstração sem entrar</Link></section></main></>}
